<h1 style="text-align:center;">Welcome To Bodacious Burgers!</h1>

<h2>Try Our Most Popular Burgers:</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    {{> burgers/burger-block devoured=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>And If You're Still Hungry....</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{> burgers/burger-block devoured=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Create Your Own Burger:</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" name="name">
    </div>

    <div class="form-group">
        <label for="devoured"></label>
        <input type="radio" name="devoured" value="1" checked>Ready To Eat!<br>
        <input type="radio" name="devoured" value="0">Make Me Another!
    </div>
    <button type="submit" id="create-burger-btn">Create Burger</button>
</form>